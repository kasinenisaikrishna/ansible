- name: loops demo
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ item }}" #item is a keyword used to hold the iterated values
    loop:
    - sachin
    - sehwag
    - dravid
    - pujara